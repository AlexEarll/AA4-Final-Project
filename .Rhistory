}
}
getMoves <- function(get.name) {
pokemon.url <- paste(base.url,"pokemon/", get.name, "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
nums <- c(1:num.rows)
if(nums > 167) {
nums <- 167
}
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 8)
for (index in nums) {
url <- toString(poke.moves[index, 2])
response <- GET(url)
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
print(paste("Loading moves",temp.data$id)
if((temp.data$id < 167) && (does.damage == "damage" || does.damage == "damage+ailment")) {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
colnames(moves) <- c("Id", "Name", "Power", "Accuracy", "Crit Rate", "Damage Class", "Type")
return(moves)
}
LoadAllImages <- funtion() {
poke.nums <- c(1:151)
if(file.exists("data/151.png")){
for(p in poke.nums) {
image.urls <- c(paste0("https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/", p, ".png"))
download.file(image.urls, destfile = paste0("img/", p, ".png"), mode = "wb")
}
}
}
getMoves <- function(get.name) {
pokemon.url <- paste(base.url,"pokemon/", get.name, "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
nums <- c(1:num.rows)
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 8)
for (index in nums) {
url <- toString(poke.moves[index, 2])
print(url)
response <- GET(url)
print(names(response))
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
if(does.damage == "damage" || does.damage == "damage+ailment") {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
print(temp.data$id)
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
colnames(moves) <- c("Id", "Name", "Power", "Accuracy", "Crit Rate", "Damage Class", "Type")
return(moves)
}
getMoves <- function(get.name) {
pokemon.url <- paste(base.url,"pokemon/", get.name, "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
if(num.rows > 167) {
num.rows <- 167
}
nums <- c(1:num.rows)
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 8)
for (index in nums) {
url <- toString(poke.moves[index, 2])
print(url)
response <- GET(url)
print(names(response))
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
if(does.damage == "damage" || does.damage == "damage+ailment") {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
print(temp.data$id)
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
colnames(moves) <- c("Id", "Name", "Power", "Accuracy", "Crit Rate", "Damage Class", "Type")
return(moves)
}
poke.2.moves <- getMoves(poke.2.name)
getMoves <- function(get.name) {
pokemon.url <- paste(base.url,"pokemon/", get.name, "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
if(num.rows > 167) {
num.rows <- 167
}
nums <- c(1:num.rows)
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 8)
for (index in nums) {
url <- toString(poke.moves[index, 2])
print(url)
response <- GET(url)
print(names(response))
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
if(does.damage == "damage" || does.damage == "damage+ailment") {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
print(temp.data$id)
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
colnames(moves) <- c("Id", "Name", "Power", "Accuracy", "Crit Rate", "Damage Class", "Type")
return(moves)
}
poke.2.moves <- getMoves(poke.2.name)
print(num.rows)
pokemon.url <- paste(base.url,"pokemon/", "mew", "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
print(num.rows)
if(num.rows > 167) {
num.rows <- 167
}
print(num.rows)
nums <- c(1:num.rows)
nums <- sample(nums, 8)
print(nums)
getMoves <- function(get.name) {
pokemon.url <- paste(base.url,"pokemon/", get.name, "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
if(num.rows > 167) {
num.rows <- 167
}
print(num.rows)
nums <- c(1:num.rows)
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 8)
for (index in nums) {
url <- toString(poke.moves[index, 2])
print(url)
response <- GET(url)
print(names(response))
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
if(does.damage == "damage" || does.damage == "damage+ailment") {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
print(temp.data$id)
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
colnames(moves) <- c("Id", "Name", "Power", "Accuracy", "Crit Rate", "Damage Class", "Type")
return(moves)
}
poke.2.moves <- getMoves(poke.2.name)
View(poke.moves)
getMoves <- function(get.name) {
pokemon.url <- paste(base.url,"pokemon/", "mew", "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
if(num.rows > 167) {
num.rows <- 167
}
print(num.rows)
nums <- c(1:num.rows)
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 8)
for (index in nums) {
url <- toString(poke.moves[index, 2])
print(url)
response <- GET(url)
print(names(response))
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
if(does.damage == "damage" || does.damage == "damage+ailment") {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
print(temp.data$id)
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
colnames(moves) <- c("Id", "Name", "Power", "Accuracy", "Crit Rate", "Damage Class", "Type")
return(moves)
}
poke.2.moves <- getMoves(poke.2.name)
pokemon.url <- paste(base.url,"pokemon/", "mew", "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
if(num.rows > 167) {
num.rows <- 167
}
print(num.rows)
nums <- c(1:num.rows)
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 8)
for (index in nums) {
url <- toString(poke.moves[index, 2])
print(url)
response <- GET(url)
print(names(response))
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
if(does.damage == "damage" || does.damage == "damage+ailment") {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
print(temp.data$id)
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
print(nums)
print(nums)
pokemon.url <- paste(base.url,"pokemon/", "mew", "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
if(num.rows > 167) {
num.rows <- 167
}
print(num.rows)
nums <- c(1:num.rows)
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 8)
print(nums)
for (index in nums) {
url <- toString(poke.moves[index, 2])
print(url)
response <- GET(url)
print(names(response))
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
if(does.damage == "damage" || does.damage == "damage+ailment") {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
print(temp.data$id)
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
View(poke.moves)
print(num.rows)
num.rows <- nrow(poke.moves)
if(num.rows > 167) {
num.rows <- 167
}
nums <- c(1:num.rows)
moves <- data.frame()
getMoves <- function(get.name) {
pokemon.url <- paste(base.url,"pokemon/", "mew", "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
if(num.rows > 167) {
num.rows <- 167
}
nums <- c(1:num.rows)
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 8)
for (index in nums) {
url <- toString(poke.moves[index, 2])
response <- GET(url)
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
if(does.damage == "damage" || does.damage == "damage+ailment") {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
print(temp.data$id)
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
colnames(moves) <- c("Id", "Name", "Power", "Accuracy", "Crit Rate", "Damage Class", "Type")
return(moves)
}
poke.2.moves <- getMoves(poke.2.name)
View(poke.2.moves)
getMoves <- function(get.name) {
pokemon.url <- paste(base.url,"pokemon/", "mew", "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
if(num.rows > 167) {
num.rows <- 167
}
nums <- c(1:num.rows)
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 10)
for (index in nums) {
url <- toString(poke.moves[index, 2])
response <- GET(url)
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
if(does.damage == "damage" || does.damage == "damage+ailment") {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
print(temp.data$id)
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
colnames(moves) <- c("Id", "Name", "Power", "Accuracy", "Crit Rate", "Damage Class", "Type")
return(moves)
}
poke.2.moves <- getMoves(poke.2.name)
poke.2.stats <- filter(first.gen, name == poke.2.name)
poke.2.hp <- poke.1.stats[1, 7]
View(poke.2.moves)
getMoves <- function(get.name) {
pokemon.url <- paste(base.url,"pokemon/", get,name, "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
if(num.rows > 167) {
num.rows <- 167
}
nums <- c(1:num.rows)
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 10)
for (index in nums) {
url <- toString(poke.moves[index, 2])
response <- GET(url)
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
if(does.damage == "damage" || does.damage == "damage+ailment") {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
print(temp.data$id)
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
colnames(moves) <- c("Id", "Name", "Power", "Accuracy", "Crit Rate", "Damage Class", "Type")
return(moves)
}
damage.table <- LoadDamageTable()
poke.1.name <- "charizard"
poke.1.moves <- head(getMoves(poke.1.name),4)
poke.1.stats <- filter(first.gen, name == poke.1.name)
poke.1.hp <- poke.1.stats[1, 7]
poke.2.name <- "mew"
poke.2.moves <- head(getMoves(poke.2.name),4)
poke.2.stats <- filter(first.gen, name == poke.2.name)
poke.2.hp <- poke.1.stats[1, 7]
View(poke.2.moves)
poke.2.moves <- head(getMoves(poke.2.name),4)
getMoves <- function(get.name) {
pokemon.url <- paste(base.url,"pokemon/", get.name, "/", sep = "")
response <- GET(pokemon.url)
body <- content(response, "text")
poke.data <- fromJSON(body)
poke.moves <- (poke.data$moves)
poke.moves <- flatten(poke.moves)
num.rows <- nrow(poke.moves)
if(num.rows > 167) {
num.rows <- 167
}
nums <- c(1:num.rows)
moves <- data.frame()
row.index <- 0
nums <- sample(nums, 10)
for (index in nums) {
url <- toString(poke.moves[index, 2])
response <- GET(url)
body <- content(response, "text")
temp.data <- fromJSON(body, flatten = TRUE)
does.damage <- temp.data$meta$category$name
if(does.damage == "damage" || does.damage == "damage+ailment") {
row.index <- row.index + 1
moves[row.index, 1] <- temp.data$id
print(temp.data$id)
moves[row.index, 2] <- temp.data$name
moves[row.index, 3] <- temp.data$power
moves[row.index, 4] <- temp.data$accuracy
moves[row.index, 5] <- temp.data$meta$crit_rate
moves[row.index, 6] <- temp.data$damage_class$name
moves[row.index, 7] <- temp.data$type$name
}
}
colnames(moves) <- c("Id", "Name", "Power", "Accuracy", "Crit Rate", "Damage Class", "Type")
return(moves)
}
poke.1.name <- "charizard"
poke.1.moves <- head(getMoves(poke.1.name),4)
poke.1.stats <- filter(first.gen, name == poke.1.name)
poke.1.hp <- poke.1.stats[1, 7]
poke.2.name <- "mew"
poke.2.moves <- head(getMoves(poke.2.name),4)
poke.2.stats <- filter(first.gen, name == poke.2.name)
poke.2.hp <- poke.1.stats[1, 7]
View(poke.2.moves)
poke.2.moves <- head(getMoves(poke.2.name), 4)
View(poke.2.moves)
poke.2.moves <- head(getMoves(poke.2.name), 4)
View(poke.2.moves)
